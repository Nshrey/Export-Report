# JobHistory Export Dashboard

Streamlit dashboard for ExportSeries JobHistory reporting.

## Setup

1. Copy config_template.py → config_local.py
2. Fill in API details
3. Install dependencies:
   pip install -r requirements.txt
4. Run:
   streamlit run app.py